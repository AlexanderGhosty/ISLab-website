-- Инициализация базы данных
-- MySQL Docker образ автоматически создает базу данных и пользователя
-- на основе переменных окружения: MYSQL_DATABASE, MYSQL_USER, MYSQL_PASSWORD

-- Этот файл выполняется после автоматического создания базы данных и пользователя
-- Здесь можно добавить дополнительные настройки, таблицы или данные

-- Убеждаемся, что пользователь имеет все необходимые права
-- (обычно это уже сделано автоматически, но для уверенности)
SET @db_name = (SELECT SCHEMA());
SET @user_name = IFNULL(@user_name, 'islab_user');

-- Применение изменений
FLUSH PRIVILEGES;

-- Можно добавить создание дополнительных таблиц или начальные данные здесь
